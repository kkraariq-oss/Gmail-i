# 🚀 دليل تثبيت التطبيق المحدث

## الملفات المطلوبة:
- ✅ index.html (الملف الرئيسي المحدث)
- ✅ manifest.json (ملف PWA)
- ✅ service-worker.js (للعمل بدون إنترنت)

## 📋 التحديثات الرئيسية:

### 1. **نظام الأصوات**
- صوت إرسال رسالة
- صوت استقبال رسالة  
- التحكم في الأصوات من الإعدادات

### 2. **نظام الإشعارات**
- زر إشعارات في الشريط العلوي
- عداد الإشعارات غير المقروءة
- لوحة إشعارات منزلقة

### 3. **تسجيل صوتي جديد**
- واجهة تسجيل احترافية
- عداد الوقت
- موجات صوتية
- حذف/إيقاف/إرسال

### 4. **معاينة الصور**
- معاينة قبل الإرسال
- تحديد صور متعددة
- إضافة تعليق
- مؤشر تحميل

### 5. **حذف رسائل متقدم**
- حذف لدي فقط
- حذف للطرفين
- تحديد متعدد

### 6. **إعدادات المستخدم**
- تغيير الصورة
- تغيير الاسم
- الأصوات
- حالة الاتصال

### 7. **تحديثات الأدمن**
- رفع صورة من الجهاز
- كود ديناميكي في قاعدة البيانات
- لوحة إحصائيات

## 🔧 خطوات التثبيت:

### 1. إعداد Firebase:
```javascript
// في ملف index.html، حدّث هذه القيم:
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    databaseURL: "YOUR_DATABASE_URL",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

### 2. رفع الملفات:
- ارفع جميع الملفات إلى خادم يدعم HTTPS
- تأكد من إعدادات Firebase

### 3. الاستخدام:
1. افتح التطبيق
2. اضغط 5 مرات على شعار Gmail
3. أدخل كود الأدمن (سيُنشأ تلقائياً في أول تشغيل)

## 📝 ملاحظات مهمة:

- التطبيق يحتاج HTTPS للكاميرا والميكروفون
- Firebase يوفر استضافة مجانية مع HTTPS
- كود الأدمن الافتراضي سيكون "ADMIN123" (غيّره فوراً)

## 🔒 الأمان:

1. غيّر كود الأدمن فوراً
2. استخدم قواعد أمان Firebase
3. لا تشارك أكواد المستخدمين
4. احتفظ بنسخ احتياطية

## 📞 الدعم:

للمشاكل التقنية:
- تحقق من Console المتصفح (F12)
- تأكد من إعدادات Firebase
- تحقق من قواعد Database و Storage

