# دليل التثبيت السريع - نظام إدارة المطاعم v3.0

## 🚀 الطريقة الأولى: تشغيل في المتصفح (الأسهل)

### خطوات التشغيل:
1. استخرج ملف `restaurant-system-v3.0.zip`
2. افتح ملف `index.html` في المتصفح
3. سجل الدخول:
   - اسم المستخدم: `admin`
   - كلمة المرور: `admin123`
4. ابدأ الاستخدام!

### المتصفحات المدعومة:
- Google Chrome 90+
- Microsoft Edge 90+
- Firefox 88+
- Safari 14+

---

## 💻 الطريقة الثانية: تطبيق Electron (موصى به)

### المتطلبات:
- Node.js 16+ و npm 8+
- نظام التشغيل: Windows 10+, macOS 10.14+, أو Linux

### خطوات التثبيت:

#### 1. تثبيت Node.js (إذا لم يكن مثبتاً)
- قم بزيارة: https://nodejs.org
- حمّل الإصدار LTS
- ثبّت Node.js

#### 2. تثبيت المشروع
```bash
# افتح Terminal أو Command Prompt في مجلد المشروع
cd restaurant-system-v3

# ثبّت المكتبات المطلوبة
npm install
```

#### 3. تشغيل التطبيق
```bash
npm start
```

#### 4. بناء التطبيق للتوزيع
```bash
# بناء لنظام Windows
npm run build:win

# بناء لنظام Mac
npm run build:mac

# بناء لنظام Linux
npm run build:linux
```

---

## 🖨️ إعداد الطابعات الحرارية

### الطابعات المدعومة:
- Epson TM-T20, TM-T82, TM-T88
- Star TSP100, TSP650, TSP700
- Xprinter XP-80mm, XP-365B
- أي طابعة حرارية بحجم 80mm

### خطوات الإعداد:

#### 1. توصيل الطابعة:
- وصّل الطابعة بالكمبيوتر عبر USB أو الشبكة
- ثبّت تعريف الطابعة من الموقع الرسمي للشركة المصنعة
- تأكد من تشغيل الطابعة

#### 2. إعداد في النظام:
1. افتح تطبيق Electron
2. اذهب إلى: **الإعدادات** > **إعدادات الطباعة**
3. حدد:
   - **طابعة المطبخ**: الطابعة المتصلة بالمطبخ
   - **طابعة الكاشير**: الطابعة المتصلة بالكاشير
4. احفظ الإعدادات
5. اختبر الطباعة من زر "طباعة تجريبية"

#### 3. اختبار الطباعة:
```
إذا نجحت الطباعة التجريبية → كل شيء جاهز! ✅
إذا فشلت → تحقق من:
  - اتصال الطابعة
  - تثبيت التعريف
  - إعدادات الطابعة في Windows/Mac
```

---

## ⚙️ الإعدادات الأولية

### 1. معلومات المطعم:
1. اذهب إلى **الإعدادات**
2. املأ:
   - اسم المطعم
   - رقم الهاتف
   - العنوان
   - (اختياري) شعار المطعم

### 2. إضافة التصنيفات:
1. اذهب إلى **المنتجات**
2. أضف تصنيفاتك (همبرجر، بيتزا، مشروبات، إلخ)

### 3. إضافة المنتجات:
1. اذهب إلى **المنتجات** > **إضافة منتج**
2. أدخل:
   - اسم المنتج
   - السعر
   - التصنيف
   - (اختياري) صورة المنتج

### 4. إنشاء مستخدمين:
1. اذهب إلى **الأمان** > **إدارة المستخدمين**
2. أضف مستخدمين جدد (كاشير، موظفين، إلخ)
3. حدد الصلاحيات لكل مستخدم

---

## 🎯 البدء في الاستخدام

### نقطة البيع:
1. اضغط على **نقطة البيع** من الشاشة الرئيسية
2. اختر التصنيف
3. **اضغط على أي منتج** لفتح نافذة الكمية
4. أدخل الكمية واضغط **إضافة للسلة**
5. كرر للمنتجات الأخرى
6. اضغط **إتمام البيع**
7. ستطبع إيصالين تلقائياً (مطبخ + كاشير)

### الإيصالات المعلقة:
1. أثناء البيع، اضغط **تعليق**
2. لاستعادة إيصال معلق، اضغط **الإيصالات المعلقة**
3. اختر الإيصال واضغط **تحميل**

---

## 🔧 حل المشاكل الشائعة

### المشكلة: لا تعمل الطباعة
**الحلول:**
- تأكد من توصيل الطابعة وتشغيلها
- أعد تثبيت تعريف الطابعة
- حاول طابعة أخرى في الإعدادات
- أعد تشغيل التطبيق

### المشكلة: لا يعمل الصوت
**الحلول:**
- تحقق من إعدادات الصوت في النظام
- فعّل الأصوات من **الإعدادات**
- جرب في متصفح آخر

### المشكلة: البيانات لا تُحفظ
**الحلول:**
- تأكد من تفعيل LocalStorage في المتصفح
- جرب في متصفح آخر
- في Electron: تحقق من صلاحيات الكتابة

### المشكلة: بطء في الأداء
**الحلول:**
- استخدم تطبيق Electron بدلاً من المتصفح
- أغلق التطبيقات الأخرى
- امسح ذاكرة التخزين المؤقت

---

## 📦 النسخ الاحتياطي

### نسخ احتياطي يدوي:
1. اذهب إلى **النسخ الاحتياطي**
2. اضغط **تصدير البيانات**
3. اختر المجلد واحفظ الملف
4. احتفظ بالملف في مكان آمن

### استعادة النسخة:
1. اذهب إلى **النسخ الاحتياطي**
2. اضغط **استيراد البيانات**
3. اختر ملف النسخة الاحتياطية (.json)
4. تأكد من الاستعادة

### نصيحة: 
- خذ نسخة احتياطية يومية
- احتفظ بنسخ في أماكن متعددة
- اختبر الاستعادة بشكل دوري

---

## 📞 الحصول على المساعدة

إذا واجهت مشاكل:
1. راجع قسم **حل المشاكل** أعلاه
2. اقرأ ملف `README-V3.md`
3. تواصل معنا:
   - Email: support@restaurant-system.com
   - Phone: 07XXXXXXXXX
   - WhatsApp: +964 XXX XXX XXXX

---

**مبروك! نظامك جاهز للعمل! 🎉**
